// This Prisma schema will define the database owned by the Learning Service.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("LEARNING_DATABASE_URL")
}

// TODO: Add only models owned by this service.

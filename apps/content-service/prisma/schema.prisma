// This Prisma schema will define the database owned by the Content Service.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("CONTENT_DATABASE_URL")
}

// TODO: Add only models owned by this service.
